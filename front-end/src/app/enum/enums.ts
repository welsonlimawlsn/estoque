export enum HttpStatusCode {
  UNDEFINED = 0,
  OK = 200,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403
}

export enum FormatoRelatorio {
  PDF = 'PDF',
  XLS = 'XLS',
  RTF = 'RTF'
}

export enum Header {
  CONTENT_DISPOSITION = 'content-disposition'
}
